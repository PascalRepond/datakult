version: '3.7'

services:
  web:
    build: .
    command: python /code/manage.py runserver 0.0.0.0:8000
    environment:
      - SECRET_KEY=j03%v0s6zu#nmf564)dva0)qg2vibb*8e#1!c%65ghx)(9ym^#
      - DEBUG=1
      - EMAIL_HOST=smtp-relay.sendinblue.com
      - EMAIL_HOST_USER=pascalr.92@gmail.com
      - EMAIL_HOST_PASSWORD=GLrDNwHSOC7YRvxk
      - EMAIL_PORT=587
      - EMAIL_USE_TLS=1
    volumes:
      - .:/code
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres:11
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment: 
      POSTGRES_DB: 'db'
      POSTGRES_PASSWORD: 'postgres'

volumes:
  postgres_data: